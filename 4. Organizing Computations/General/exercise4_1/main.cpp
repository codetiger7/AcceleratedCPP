// Exercise 4-1
// Will the following code work?
// no it will not work, because s.name.size() returns the type: string::size_type
// while the maxlen is an int

// I would fix it by making maxlen of size string::sizetype

#include <string>
#include <algorithm>
#include <iostream>

using namespace std;

int main()
{

    return 0;
}

